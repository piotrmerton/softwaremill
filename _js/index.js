//index related logic, will be included in main bundle for now


import { partners } from './partials/partners';

document.addEventListener("DOMContentLoaded", () => { 
    
    partners.initSlider();

} );